opencv-python==4.10.0.84
tensorboard
--extra-index-url https://download.pytorch.org/whl/cu129
torch
torchvision
mujoco>=3.1
# Pin to a commit SHA and ALSO bump homebot-3d's version on every change: pip
# reports a git dep "already satisfied" if the installed package name+version
# matches, so a SHA change alone won't reinstall — the version must differ too.
# 30439f1 = v0.1.1, ships assets/textures PNGs in non-editable installs.
git+ssh://git@github.com/bobcowher/homebot-3d.git@30439f124855110ba9941e87f1e22f6f6b6c7576
